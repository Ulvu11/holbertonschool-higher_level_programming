This project implements a function that safely prints a specified number of elements from a list using try...except blocks.
It ensures that the program handles out-of-range errors gracefully without crashing, even when the requested count exceeds the list length.
